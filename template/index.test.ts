import { assertEquals } from "@std/assert/equals";
import { part1, part2 } from "./index.ts";

const testInput = ``;

Deno.test("part 1: ", () => {
  assertEquals(part1(testInput), true);
});

Deno.test("part 2: ", () => {
  assertEquals(part2(testInput), true);
});
